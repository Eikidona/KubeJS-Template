/**
 * @class
 * @classdesc
 * @param {$EventBus} eventBus 
 * @param {$ModuleContainer} ModuleContainer 
 */
function $Modpack(eventBus, ModuleContainer) {
  // eventBus.addListener()
}

$Main.addModule($Modpack);